import { css } from '@emotion/react';

const layout = css`
  width: 100%;
  height: calc(100vh - 64px);
  display: flex;
  justify-content: center;
`;

const styles = { layout };

export default styles;
