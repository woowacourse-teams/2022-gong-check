import { css } from '@emotion/react';

const layout = css`
  width: 100%;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
`;

const styles = { layout };

export default styles;
