<?xml version="1.0" encoding="UTF-8"?>

<!--기본 프로퍼티 설정-->
<included>
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%logger.%M){cyan} %clr(:%n>){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
    <property name="CONSOLE_DB_LOG_PATTERN" value="%blue(> %msg%n)"/>
    <property name="CONSOLE_QUERY_LOG_PATTERN" value="%blue(> [query] %msg%n)"/>

    <property name="FILE_LOG_PATTERN"
              value="${FILE_LOG_PATTERN:-%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} ${LOG_LEVEL_PATTERN:-%5p} ${PID:- } --- [%t] %-40.40logger.%M{39} :%n> %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <property name="ACCESS_LOG_PATTERN"
              value="--------------------------------------%n###### HTTP Request ###### %n%fullRequest###### HTTP Response ###### %n%fullResponse"/>

    <property name="UTF8_LOG_CHARSET" value="${CONSOLE_LOG_CHARSET:-${file.encoding:-UTF-8}}"/>
</included>
