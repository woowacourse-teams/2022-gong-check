spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  sql:
    init:
      schema-locations: classpath:schema.sql
      data-locations: classpath:dummy_data.sql
  output:
    ansi:
      enabled: always
  profiles:
    default: local
